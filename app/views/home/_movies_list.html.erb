<section class="full-width">
  <div class="container panel-white">
		<div class="col-centered">
        <div class="tab-nav-container" id="infinite_movies_listing" data-id="<%= "#{@next_index}" %>">
          <%= render :partial=> "/home/infinite_movies_listing",:locals=>{:movies=>movies} %>
        </div>
    </div>
  </div>
</section>